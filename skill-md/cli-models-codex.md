---
name: cli-models
display_name: cli-models
platform: Codex
category: General and specialized workflows
---

# cli-models - Codex Skill Package

## What This Is

This is a friend-safe Markdown copy of `cli-models` for Codex. It removes local filesystem paths, Finder-only links, and machine-specific source locations.

## When To Use This Skill

Use this skill when your task matches this description:

Query available AI models, list model aliases, and browse the full model catalog from the CLI. Filter by provider, search by capability, and resolve model name variants.

## How To Use It In Codex

In Codex, click the chat box, press /, choose cli-models, then write the task. Fallback prompt: Use the cli-models skill to...

## Skill Metadata

| Field | Value |
| --- | --- |
| Display name | `cli-models` |
| Canonical name | `cli-models` |
| Platform | `Codex` |
| Category | General and specialized workflows |

## Description

Query available AI models, list model aliases, and browse the full model catalog from the CLI. Filter by provider, search by capability, and resolve model name variants.


## Original SKILL.md

---
name: cli-models
description: Query available AI models, list model aliases, and browse the full model catalog from the CLI. Filter by provider, search by capability, and resolve model name variants.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Query available AI models, list model aliases, and browse the full model catalog from the CLI. Filter by provider, search by capability, and resolve model name variants.

## Quick install

```bash
npm install -g omniroute   # or: npx omniroute
omniroute --version
```

## Subcommands

### `models [provider]`

**Flags:**

- `--search <query>`
- `--json`

**Example:**

```bash
omniroute models [provider]
```
