---
name: omni-db-backups
display_name: omni-db-backups
platform: Codex
category: General and specialized workflows
---

# omni-db-backups - Codex Skill Package

## What This Is

This is a friend-safe Markdown copy of `omni-db-backups` for Codex. It removes local filesystem paths, Finder-only links, and machine-specific source locations.

## When To Use This Skill

Use this skill when your task matches this description:

Trigger system backups, restore from backup files, and manage the SQLite database lifecycle. Supports export, import, and incremental snapshot strategies.

## How To Use It In Codex

In Codex, click the chat box, press /, choose omni-db-backups, then write the task. Fallback prompt: Use the omni-db-backups skill to...

## Skill Metadata

| Field | Value |
| --- | --- |
| Display name | `omni-db-backups` |
| Canonical name | `omni-db-backups` |
| Platform | `Codex` |
| Category | General and specialized workflows |

## Description

Trigger system backups, restore from backup files, and manage the SQLite database lifecycle. Supports export, import, and incremental snapshot strategies.


## Original SKILL.md

---
name: omni-db-backups
description: Trigger system backups, restore from backup files, and manage the SQLite database lifecycle. Supports export, import, and incremental snapshot strategies.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Trigger system backups, restore from backup files, and manage the SQLite database lifecycle. Supports export, import, and incremental snapshot strategies.

## Authentication

All requests require a valid Bearer token or session cookie. Obtain a token via `POST /api/auth/login` or configure `REQUIRE_API_KEY=false` for local development.

## Endpoints

_No endpoints mapped for this area yet._
## Payloads

See the full OpenAPI specification at `GET /api/openapi/spec` or `docs/openapi.yaml` for detailed request/response schemas.
