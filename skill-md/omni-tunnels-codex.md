---
name: omni-tunnels
display_name: omni-tunnels
platform: Codex
category: General and specialized workflows
---

# omni-tunnels - Codex Skill Package

## What This Is

This is a friend-safe Markdown copy of `omni-tunnels` for Codex. It removes local filesystem paths, Finder-only links, and machine-specific source locations.

## When To Use This Skill

Use this skill when your task matches this description:

Create and manage secure tunnels (ngrok, Cloudflare Tunnel, custom) to expose OmniRoute to the internet or share access with remote agents and CI pipelines.

## How To Use It In Codex

In Codex, click the chat box, press /, choose omni-tunnels, then write the task. Fallback prompt: Use the omni-tunnels skill to...

## Skill Metadata

| Field | Value |
| --- | --- |
| Display name | `omni-tunnels` |
| Canonical name | `omni-tunnels` |
| Platform | `Codex` |
| Category | General and specialized workflows |

## Description

Create and manage secure tunnels (ngrok, Cloudflare Tunnel, custom) to expose OmniRoute to the internet or share access with remote agents and CI pipelines.


## Original SKILL.md

---
name: omni-tunnels
description: Create and manage secure tunnels (ngrok, Cloudflare Tunnel, custom) to expose OmniRoute to the internet or share access with remote agents and CI pipelines.
---
<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Create and manage secure tunnels (ngrok, Cloudflare Tunnel, custom) to expose OmniRoute to the internet or share access with remote agents and CI pipelines.

## Authentication

All requests require a valid Bearer token or session cookie. Obtain a token via `POST /api/auth/login` or configure `REQUIRE_API_KEY=false` for local development.

## Endpoints

_No endpoints mapped for this area yet._
## Payloads

See the full OpenAPI specification at `GET /api/openapi/spec` or `docs/openapi.yaml` for detailed request/response schemas.
